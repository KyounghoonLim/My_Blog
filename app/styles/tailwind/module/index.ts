import animations from './_animations.json'
import keyframes from './_keyframes.json'
import colors from './_colors.json'
import breakpoints from './_breakpoints.json'

const modules = {
  animation: animations,
  keyframes,
  colors,
  screens: breakpoints,
}

export default modules
